package util;

import java.time.LocalDate;
import java.time.temporal.ChronoUnit;

public class Multa {

	public long DiasdeMulta(LocalDate dataEntrega, LocalDate dataDevolvida) {
		long diasMultas = ChronoUnit.DAYS.between(dataEntrega, dataDevolvida);
		return diasMultas;
	}
}
